apiVersion: v1
kind: Service
metadata:
  name: db-service
spec:
  clusterIP: "None"
  ports:
  - port: 3306
    protocol: TCP
    targetPort: 3306
  selector:
    app: db-statefulset
  type: ClusterIP

