apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: movie-backend
spec:
  maxReplicas: 5
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: movie-backend
  targetCPUUtilizationPercentage: 70
