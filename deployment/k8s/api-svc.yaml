apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: movie-reservation
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8000
    nodePort: 30080
  selector:
    app: movie-backend
  type: NodePort

